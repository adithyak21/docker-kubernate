sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t adithya-tech/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true adithya-tech/react-test npm test

after_success:
  - docker build -t adithya-tech/multi-client ./client
  - docker build -t adithya-tech/multi-nginx ./nginx
  - docker build -t adithya-tech/multi-server ./server
  - docker build -t adithya-tech/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push adithya-tech/multi-client
  - docker push adithya-tech/multi-nginx
  - docker push adithya-tech/multi-server
  - docker push adithya-tech/multi-worker
